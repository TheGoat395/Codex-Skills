interface:
  display_name: "Website Operating Rules"
  short_description: "Core workflow for non-generic website builds"
  default_prompt: "Use $website-operating-rules to own the shared web execution and evidence contract within the requested scope."
