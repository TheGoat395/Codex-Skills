interface:
  display_name: "React Responsive Components"
  short_description: "Build React-aware responsive behavior and composition"
  default_prompt: "Use $react-responsive-components to build React-aware responsive behavior and composition, matching the requested scope and existing project."
