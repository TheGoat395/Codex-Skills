interface:
  display_name: "Visual Regression Lab"
  short_description: "Verify rendered UI and complex full-page captures"
  default_prompt: "Use $visual-regression-lab to inspect this site across viewports and use stitched capture if one-shot full-page evidence is unreliable."
