interface:
  display_name: "Motion React Layout Transitions"
  short_description: "Implement Motion layout and shared-element transitions"
  default_prompt: "Use $motion-react-layout-transitions to implement Motion layout and shared-element transitions, matching the requested scope and existing project."
