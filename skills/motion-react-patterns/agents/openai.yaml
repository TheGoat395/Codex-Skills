interface:
  display_name: "Motion React Patterns"
  short_description: "Apply Motion APIs with sound React lifecycle ownership"
  default_prompt: "Use $motion-react-patterns to apply Motion APIs with sound React lifecycle ownership, matching the requested scope and existing project."
