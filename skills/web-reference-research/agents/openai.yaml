interface:
  display_name: "Web Reference Research"
  short_description: "Turn website references into build rules"
  default_prompt: "Use $web-reference-research to translate selected external evidence into build rules within the requested scope."
