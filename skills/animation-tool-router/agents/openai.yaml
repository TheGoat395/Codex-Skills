interface:
  display_name: "Animation Tool Router"
  short_description: "Choose motion tools for the actual interaction"
  default_prompt: "Use $animation-tool-router to choose motion tools for the actual interaction, matching the requested scope and existing project."
