interface:
  display_name: "React Accessible Components"
  short_description: "Build React semantics, keyboard and focus behavior"
  default_prompt: "Use $react-accessible-components to build React semantics, keyboard and focus behavior, matching the requested scope and existing project."
