interface:
  display_name: "React Component Composition"
  short_description: "Shape React component boundaries, props and slots"
  default_prompt: "Use $react-component-composition to shape React component boundaries, props and slots, matching the requested scope and existing project."
