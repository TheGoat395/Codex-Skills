interface:
  display_name: "Content Pruning Review"
  short_description: "Remove weak, redundant, generic, or unsupported website content"
  default_prompt: "Use $content-pruning-review to remove content that dilutes the reader's task within the requested scope."
