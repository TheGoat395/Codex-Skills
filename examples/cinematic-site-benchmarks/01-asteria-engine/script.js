(()=>{const r=window.matchMedia('(prefers-reduced-motion: reduce)').matches,s=document.getElementById('scene-canvas'),p=document.getElementById('particles-canvas'),c=s.getContext('2d'),x=p.getContext('2d');let w=0,h=0,d=1,ps=[];function resize(){d=Math.min(window.devicePixelRatio||1,2);w=window.innerWidth;h=window.innerHeight;for(const a of[s,p]){a.width=Math.round(w*d);a.height=Math.round(h*d);a.style.width=`${w}px`;a.style.height=`${h}px`}c.setTransform(d,0,0,d,0,0);x.setTransform(d,0,0,d,0,0);makeParticles()}function makeParticles(){const n=Math.max(42,Math.min(140,Math.floor(w*h/13500)));ps=Array.from({length:n},()=>({x:Math.random()*w,y:Math.random()*h,z:Math.random(),vx:(Math.random()-.5)*.14,vy:(Math.random()-.5)*.14,r:Math.random()*1.6+.35,a:Math.random()*.55+.18}))}function scene(t){t*=.00022;const sc=window.scrollY/Math.max(1,document.documentElement.scrollHeight-h);c.clearRect(0,0,w,h);const g=c.createRadialGradient(w*(.48+Math.sin(t)*.04),h*(.36+sc*.24),20,w*.5,h*.5,Math.max(w,h)*.86);g.addColorStop(0,`rgba(100,168,255,${.28+sc*.16})`);g.addColorStop(.28,'rgba(34,211,238,.11)');g.addColorStop(.62,'rgba(91,33,182,.10)');g.addColorStop(1,'rgba(2,3,10,.96)');c.fillStyle=g;c.fillRect(0,0,w,h);const cx=w*.5,cy=h*(.42+sc*.18),base=Math.min(w,h)*(.22+sc*.08);c.save();c.translate(cx,cy);c.rotate(t*1.5+sc*Math.PI*.85);for(let i=0;i<7;i++){const rx=base*(1+i*.22),ry=base*(.34+i*.045);c.beginPath();c.ellipse(0,0,rx,ry,0,0,Math.PI*2);c.strokeStyle=`rgba(180,218,255,${.17-i*.015})`;c.lineWidth=Math.max(1,1.8-i*.12);c.stroke()}for(let i=0;i<4;i++){const a=t*(1.8+i*.24)+i*1.4+sc*4,xx=Math.cos(a)*base*(1.2+i*.28),yy=Math.sin(a)*base*(.34+i*.06),gl=c.createRadialGradient(xx,yy,0,xx,yy,42);gl.addColorStop(0,i%2?'rgba(120,255,228,.9)':'rgba(100,168,255,.92)');gl.addColorStop(1,'rgba(100,168,255,0)');c.fillStyle=gl;c.beginPath();c.arc(xx,yy,42,0,Math.PI*2);c.fill()}c.restore();c.fillStyle=`rgba(2,3,10,${.18+sc*.55})`;c.fillRect(0,0,w,h)}function drawParticles(){x.clearRect(0,0,w,h);for(const q of ps){q.x+=q.vx*(.6+q.z);q.y+=q.vy*(.6+q.z);if(q.x<-10)q.x=w+10;if(q.x>w+10)q.x=-10;if(q.y<-10)q.y=h+10;if(q.y>h+10)q.y=-10;x.beginPath();x.arc(q.x,q.y,q.r,0,Math.PI*2);x.fillStyle=`rgba(235,248,255,${q.a})`;x.fill()}}function scrollFx(){const hero=document.querySelector('.hero-content'),cards=document.getElementById('feature-cards'),stage=document.getElementById('systems'),fade=Math.max(0,1-window.scrollY/(window.innerHeight*.62));hero.style.opacity=fade.toFixed(3);hero.style.transform=`translateY(${(1-fade)*-24}px)`;hero.style.filter=`blur(${(1-fade)*8}px)`;const rect=stage.getBoundingClientRect(),start=window.innerHeight*.7,end=-rect.height*.32,prog=Math.max(0,Math.min(1,(start-rect.top)/(start-end)));cards.style.setProperty('--reveal',`${Math.round(prog*132)}%`);cards.style.setProperty('--card-lift',`${Math.sin(prog*Math.PI)*-22}px`)}function reveal(){const rel=document.getElementById('release-reveal'),obs=new IntersectionObserver(([e])=>{if(e.isIntersecting){rel.classList.add('visible');obs.disconnect()}},{threshold:.22});obs.observe(rel)}function tick(t){scene(t);drawParticles();scrollFx();requestAnimationFrame(tick)}resize();window.addEventListener('resize',resize,{passive:true});window.addEventListener('scroll',scrollFx,{passive:true});reveal();if(r){scrollFx()}else{requestAnimationFrame(tick)}})();
